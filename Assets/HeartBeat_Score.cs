using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeartBeat_Score : MonoBehaviour
{
    // int heartBeat;
    // Start is called before the first frame update
    void Start()
    {
        // heartBeat = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
